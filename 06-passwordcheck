user= input("enter first name:").lower()  #BÜYÜK KÜÇÜK HARF DUYARLI DEĞİL github= 06
yours = "ayhan"
if user in yours:
  print("Hello, ayhan The password is : W@12")
else:
  print(f"Hello, {user} See you later.")
